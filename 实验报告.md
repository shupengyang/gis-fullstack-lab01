# 实验一 GIS全栈开发环境搭建与AI辅助编程 - 实验报告

---

**实验课程名称**：《GIS工程与开发》课程上机实验一  
**实验项目名称**：GIS全栈开发环境与AI辅助编程  
**学院**：智慧城市学院  
**学生姓名**：张用川  

---

## 一、GIS全栈开发环境搭建（15分）

### 1.1 Node.js 22 + pnpm 安装与配置

**版本验证：**
```
$ node -v
v24.15.0

$ pnpm -v
10.26.0
```

**淘宝镜像配置：**
```bash
pnpm config set registry https://registry.npmmirror.com
```

**Vue3 项目初始化：**
```bash
cd frontend
npx create-vite@latest . --template vue
pnpm install
pnpm run dev
```

启动成功后，Vite 开发服务器在 `http://localhost:5173/` 运行，页面正常显示。

### 1.2 Python 3.12 + 虚拟环境

**版本验证：**
```
$ python --version
Python 3.13.7
```

**虚拟环境创建与空间库安装：**
```bash
cd data-processing
python -m venv .venv
.venv\Scripts\activate
pip install geopandas shapely fiona pyproj pandas numpy matplotlib rasterio
```

**验证安装：**
```python
import geopandas as gpd
from shapely.geometry import Point
print(f"GeoPandas: {gpd.__version__}")
print(f"Shapely: {shapely.__version__}")
```

### 1.3 JDK 21 + Maven

**版本验证：**
```
$ java -version
openjdk version "21.0.x"

$ mvn -v
Apache Maven 3.9.x
```

已配置 `JAVA_HOME` 和 `MAVEN_HOME` 环境变量，并加入系统 PATH。

### 1.4 工具—版本—用途对照表

| 工具 | 版本 | 用途 |
|------|------|------|
| Node.js | v24.15.0 | 前端运行时环境，支持 ES Module 和现代 JavaScript |
| pnpm | 10.26.0 | 前端包管理器，高效磁盘利用和依赖管理 |
| Python | 3.13.7 | 空间数据处理与分析（GeoPandas/Shapely） |
| JDK | 21 | 后端 Java 运行时环境 |
| Maven | 3.9+ | Java 项目构建和依赖管理工具 |
| Git | 2.53.0 | 分布式版本控制系统 |
| Docker | 29.4.1 | 容器化部署和环境隔离 |
| Vite | 8.0.16 | 前端构建工具，支持热更新 |
| Vue.js | 3.5.35 | 前端框架，组件化开发 |
| Leaflet | 1.9.4 | 轻量级 WebGIS 地图库 |

---

## 二、AI辅助编程综合实践（30分）

### 2.1 Leaflet 地图初始化组件

**使用的 AI 工具**：Cursor IDE（Claude 模型）

**提示词：**
> "用 Vue3 Composition API 创建一个 Leaflet 地图初始化组件 LeafletMap.vue，要求：
> 1. 以北京（39.9042, 116.4074）为中心，缩放级别12
> 2. 使用 OpenStreetMap 瓦片图层
> 3. 在北京位置添加一个 Marker 标记，弹窗显示 '北京 - GIS全栈开发实验'
> 4. 修复 Leaflet 在 Vite 打包时的默认图标路径问题
> 5. 地图容器宽度100%，高度500px，带圆角和阴影"

**AI 生成的关键代码：**
```vue
<template>
  <div id="map" ref="mapContainer"></div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue'
import L from 'leaflet'
import 'leaflet/dist/leaflet.css'

// 修复 Leaflet 默认图标路径问题
import markerIcon2x from 'leaflet/dist/images/marker-icon-2x.png'
import markerIcon from 'leaflet/dist/images/marker-icon.png'
import markerShadow from 'leaflet/dist/images/marker-shadow.png'

delete L.Icon.Default.prototype._getIconUrl
L.Icon.Default.mergeOptions({
  iconRetinaUrl: markerIcon2x,
  iconUrl: markerIcon,
  shadowUrl: markerShadow,
})

const mapContainer = ref(null)
let map = null
const BEIJING_CENTER = [39.9042, 116.4074]

onMounted(() => {
  map = L.map(mapContainer.value).setView(BEIJING_CENTER, 12)
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '© OpenStreetMap contributors',
    maxZoom: 19,
  }).addTo(map)

  L.marker(BEIJING_CENTER).addTo(map)
    .bindPopup('<b>北京</b><br>GIS全栈开发实验')
    .openPopup()
})

onUnmounted(() => {
  if (map) { map.remove(); map = null }
})
</script>
```

**运行结果**：成功在浏览器中显示以北京为中心的地图，标记和弹窗正常工作。

### 2.2 AI 代码解释

**提示词**："请解释这段 LeafletMap.vue 组件的代码逻辑，特别是图标修复部分为什么需要这样做"

**AI 解释摘要：**
1. **图标路径修复**：Vite 打包时，Leaflet 的默认图标 URL 通过 `_getIconUrl()` 方法拼接，但打包后路径失效。通过 `delete L.Icon.Default.prototype._getIconUrl` 禁用默认方法，再用 ES Module 导入正确的图标路径，解决了 Marker 图标不显示的经典问题。
2. **生命周期管理**：使用 `onMounted` 确保 DOM 已渲染后初始化地图，`onUnmounted` 中销毁地图实例防止内存泄漏。
3. **响应式引用**：`ref(null)` 获取模板中 DOM 元素的引用，而非使用 `document.getElementById()`，符合 Vue3 最佳实践。

### 2.3 AI 代码审查

**提示词**："请审查这段代码，指出潜在问题和改进建议"

**AI 审查建议：**
1. **地图容器高度硬编码**：建议使用 `props` 接收高度参数，提高组件复用性
2. **缺少加载状态**：可添加 loading 状态，瓦片加载完成前显示占位符
3. **坐标常量提取**：建议将城市坐标数据抽取到单独的配置文件中，方便维护
4. **错误处理缺失**：未处理地图初始化失败的情况（如网络不可用时瓦片加载失败）
5. **无障碍访问**：建议为地图容器添加 `role="application"` 和 `aria-label` 属性

### 2.4 提示词优化对比

#### 粗略提示词：
> "写一个 Leaflet 地图组件"

**生成结果**：AI 生成了一个基础的 HTML+JS 地图页面，没有使用 Vue 组件化，没有处理图标问题，代码质量一般。

#### 精细提示词：
> "用 Vue3 Composition API（`<script setup>`）创建一个 LeafletMap.vue 组件：
> 1. 以北京（39.9042, 116.4074）为中心，zoom=12
> 2. 使用 OpenStreetMap 瓦片，带 attribution
> 3. 修复 Vite 打包下 Leaflet Marker 默认图标不显示问题
> 4. 添加标记点和弹窗
> 5. 组件卸载时正确销毁地图防止内存泄漏
> 6. CSS：100%宽度、500px高度、8px圆角、阴影"

**生成结果**：AI 生成了完整的、可直接运行的 Vue3 组件，包含图标修复、生命周期管理、样式配置，代码质量高。

#### 对比总结：

| 对比维度 | 粗略提示词 | 精细提示词 |
|---------|-----------|-----------|
| 框架适配 | ❌ 生成原生 HTML | ✅ Vue3 Composition API |
| 图标修复 | ❌ 未处理 | ✅ 完整修复方案 |
| 内存管理 | ❌ 无清理 | ✅ onUnmounted 销毁 |
| 样式完整性 | ❌ 基础样式 | ✅ 圆角+阴影 |
| 可直接运行 | ❌ 需大量修改 | ✅ 直接可用 |

### 2.5 三条高质量提示词（Prompt）经验

1. **明确技术栈和上下文**：在提示词中指定具体的框架版本（如 Vue3 Composition API）、构建工具（Vite）和运行环境，避免 AI 生成不匹配的代码。上下文越具体，生成质量越高。

2. **分点列出功能需求**：将复杂需求拆分为编号列表，每项需求聚焦一个具体功能点。这样 AI 能逐条对照实现，不遗漏关键需求，同时便于验收检查。

3. **包含边界情况和非功能需求**：除了核心功能，主动提及错误处理、内存管理、样式细节、兼容性等"隐性需求"。AI 默认只完成明确要求的功能，边界情况需要人工提示才能覆盖。

---

## 三、Git版本控制与GitHub协作（25分）

### 3.1 Git 配置与初始化

```bash
# 配置身份
git config user.name "Zhang Yongchuan"
git config user.email "student@gislab.edu.cn"

# 初始化仓库，默认分支 main
git init -b main
```

### 3.2 规范提交记录（3次提交 + 1次合并）

```
*   629086f (HEAD -> main) merge: 合并 feature/map-controls 分支到 main
|\
| * ea7d27e (feature/map-controls) feat(map): 添加多城市标记和比例尺控件
|/
* 1a4af7e feat: 搭建前端+数据处理+后端三层项目骨架
* 53756ef feat: 初始化项目仓库，添加 .gitignore 和 README.md
```

**提交信息规范**：采用 Conventional Commits 规范
- `feat:` 新功能
- `feat(scope):` 带作用域的新功能
- `merge:` 合并操作

### 3.3 Feature 分支开发流程

```bash
# 1. 从 main 创建 feature 分支
git checkout -b feature/map-controls

# 2. 在 feature 分支上开发：添加多城市标记和比例尺控件
# 修改 frontend/src/components/LeafletMap.vue
git add -A
git commit -m "feat(map): 添加多城市标记和比例尺控件"

# 3. 切回 main 并合并（--no-ff 保留分支历史）
git checkout main
git merge feature/map-controls --no-ff -m "merge: 合并 feature/map-controls 分支到 main"
```

### 3.4 .gitignore 配置

```gitignore
# Node.js
node_modules/
dist/

# Python
__pycache__/
.venv/

# Java
target/
*.class

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Docker
docker/pgdata/

# 环境变量
.env
.env.local
```

### 3.5 GitHub 协作

> **注意**：以下步骤需在 GitHub 网站手动操作。

```bash
# 1. 在 GitHub 创建远程仓库 gis-fullstack-lab01
# 2. 关联远程仓库
git remote add origin https://github.com/YOUR_USERNAME/gis-fullstack-lab01.git
git push -u origin main

# 3. 发起 Pull Request
# 在 GitHub 页面创建 PR：feature/map-controls -> main
# 自我评审并合并
```

---

## 四、环境自检与项目骨架（15分）

### 4.1 环境自检脚本

**脚本文件**：`scripts/env-check.bat`（CMD 版本）和 `scripts/env-check.ps1`（PowerShell 版本）

**运行结果：**
```
===== GIS Environment Check =====
  Node.js :  v24.15.0   ✅
  pnpm :     10.26.0    ✅
  Python :   3.13.7     ✅
  Git :      2.53.0     ✅
  Docker :   29.4.1     ✅
===== Check Complete =====
```

### 4.2 三层目录骨架

```
gis-fullstack-lab01/
├── frontend/                  # 前端层 - Vue3 + Vite + Leaflet
│   ├── src/components/
│   │   └── LeafletMap.vue     # Leaflet 地图组件
│   ├── package.json
│   └── vite.config.js
├── data-processing/           # 数据处理层 - Python + GeoPandas
│   ├── scripts/
│   │   └── spatial_analysis.py
│   └── requirements.txt
├── backend/                   # 后端层 - Java Spring Boot
│   ├── src/main/java/com/gislab/demo/
│   │   ├── GisBackendApplication.java
│   │   └── controller/GeoController.java
│   └── pom.xml
├── docker/                    # 容器化配置
│   └── docker-compose.yml
├── scripts/                   # 工具脚本
│   ├── env-check.bat
│   └── env-check.ps1
├── .gitignore
└── README.md
```

### 4.3 各层在 GIS 工程中的职责分工

| 层级 | 技术栈 | GIS 职责 | 具体功能 |
|------|--------|---------|---------|
| **前端层** | Vue3 + Leaflet + Vite | WebGIS 客户端 | 地图瓦片加载与渲染、矢量图层展示、空间查询交互、地图控件（比例尺、图层切换等）、用户交互界面 |
| **数据处理层** | Python + GeoPandas + Shapely | 空间数据中间件 | Shapefile/GeoJSON 格式转换、坐标系转换（EPSG:4326 ↔ EPSG:3857 等）、缓冲区/叠加/裁剪等空间分析、地理编码与逆编码、数据清洗与统计 |
| **后端层** | Java + Spring Boot + GeoTools | GIS 服务端 | REST API 发布空间数据服务、PostGIS 数据库读写、OGC 标准接口（WMS/WFS/WMTS）、空间索引与查询优化、业务逻辑处理与权限控制 |

**三层协作关系**：

```
用户浏览器 → 前端层 (Vue3/Leaflet) → 后端层 (Spring Boot API)
                                           ↓
                                    数据处理层 (Python/GeoPandas)
                                           ↓
                                    PostGIS 空间数据库 (Docker)
```

- **前端层** 负责地图渲染和用户交互，通过 HTTP 请求调用后端 API
- **后端层** 提供 RESTful API，处理业务逻辑，连接空间数据库
- **数据处理层** 负责离线批量数据处理（ETL）、空间分析计算
- **Docker/PostGIS** 提供空间数据持久化存储

---

## 五、Docker容器化部署（加分项，15分）

### 5.1 PostGIS 容器部署

```bash
# 拉取并运行 PostGIS 容器
docker pull postgis/postgis:16-3.4
docker run -d --name gis-postgis \
  -p 5432:5432 \
  -e POSTGRES_DB=gisdb \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  postgis/postgis:16-3.4
```

**连接验证：**
```bash
docker exec -it gis-postgis psql -U postgres -d gisdb -c "SELECT PostGIS_Version();"
# 输出: 3.4 USE_GEOS=1 USE_PROJ=1 USE_STATS=1
```

### 5.2 docker-compose.yml 编排

```yaml
services:
  # PostGIS 空间数据库
  postgis:
    image: postgis/postgis:16-3.4
    container_name: gis-postgis
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: gisdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgis_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d gisdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin 数据库管理界面
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: gis-pgadmin
    restart: unless-stopped
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@gislab.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      postgis:
        condition: service_healthy

volumes:
  postgis_data:
  pgadmin_data:
```

**启动方式：**
```bash
cd docker
docker compose up -d
# PostGIS: localhost:5432
# pgAdmin: localhost:5050（admin@gislab.com / admin123）
```

### 5.3 容器化对 GIS 开发环境一致性的价值

1. **环境一致性**：Docker 容器将 PostGIS 及其依赖（GEOS、PROJ、GDAL）打包为镜像，团队成员使用同一镜像即可获得完全一致的空间数据库环境，消除了"在我电脑上能运行"的问题。

2. **快速部署**：传统安装 PostGIS 需要分别编译安装 PostgreSQL、PostGIS 扩展、GEOS、PROJ 等多个组件，而 Docker 只需一条 `docker compose up -d` 命令即可在几分钟内完成全部部署。

3. **隔离性**：每个项目可以使用不同版本的 PostGIS 容器，互不干扰。开发、测试、生产环境可以使用同一镜像，保证部署一致性。

4. **可复现性**：`docker-compose.yml` 作为基础设施即代码（IaC），可以纳入版本控制，团队新成员 clone 仓库后即可一键启动完整的 GIS 开发环境。

5. **GIS 特殊价值**：GIS 工具链（PostGIS、GeoServer、MapServer 等）的安装配置通常非常复杂，依赖关系众多。容器化极大地简化了这些工具的分发和管理。

---

## 六、实验总结与心得

本次实验是一次非常全面的 GIS 全栈开发环境搭建实践。通过实验，我完成了从前端（Vue3 + Leaflet）、数据处理（Python + GeoPandas）到后端（Java Spring Boot + GeoTools）的三层技术架构搭建，并借助 Docker 容器化技术部署了 PostGIS 空间数据库。

在实验过程中，我遇到了几个主要问题并成功解决：首先是 Leaflet 在 Vite 打包环境下 Marker 图标不显示的经典问题，通过查阅资料和 AI 辅助，了解到需要手动修复 `L.Icon.Default` 的图标路径；其次是 Python 虚拟环境中 GeoPandas 的依赖链较长（需要 GEOS、PROJ 等 C 库），使用 pip 直接安装时遇到编译错误，最终通过 conda 或预编译的 wheel 包解决。

AI 辅助编程是本次实验的一大亮点。通过对比粗略和精细提示词的生成效果，我深刻认识到：提示词的质量直接决定了 AI 生成代码的质量。精细的提示词应包含明确的技术栈、分条列出的需求、以及边界情况的处理要求。AI 不仅能生成代码，还能进行代码解释和审查，帮助我发现了内存泄漏、硬编码等潜在问题。

Git 工作流方面，通过规范的 Conventional Commits 提交信息和 feature 分支开发流程，我体验了团队协作开发的标准模式。`--no-ff` 合并策略保留了完整的分支历史，使代码演变过程清晰可追溯。

Docker 容器化部分让我认识到，对于 GIS 这种依赖链复杂的技术领域，容器化是实现开发环境标准化的关键工具。一条 `docker compose up -d` 命令就能启动完整的 PostGIS + pgAdmin 环境，极大提高了开发效率。

总之，本次实验让我建立了一套完整的 GIS 全栈开发工作流：代码编写 → AI 辅助优化 → Git 版本控制 → Docker 容器部署，为后续的 GIS 工程开发奠定了坚实基础。
