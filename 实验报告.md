
# 智慧城市学院

# 学 生 实 验 报 告

**实验课程名称**：《GIS工程与开发》课程上机实验一  
**实验项目名称**：GIS全栈开发环境与AI辅助编程  
**开课实验室**：地理空间信息工程实验室  
**学院**：智慧城市学院 &emsp; 年级：2023 &emsp; 专业（1-3 班）  
**学生姓名**：张用川 &emsp; 学号：  
**开课时间**：2025 至 2026 学年第 2 学期  

---

# 实验一 GIS全栈开发环境搭建与AI辅助编程

## 【实验目标】

- 搭建覆盖前端（Node 22/pnpm）、空间数据（Python 3.12）、后端（JDK 21/Maven）的 GIS 全栈开发环境
- 熟练运用 AI 原生 IDE（Cursor、Trae、Qoder）进行代码生成、解释、审查与提示词优化
- 掌握 Git 版本控制工作流与 GitHub 协作（分支、合并、Pull Request），建立规范工程仓库
- 了解 Docker 容器化思想，能用容器快速部署 GIS 基础服务（如 PostGIS）

## 【实验要求】

- 推荐使用 AI 辅助编程工具（Cursor、Trae、Qoder IDE 等）完成实验，也可使用 VS Code、PyCharm 等传统环境
- 本实验在第 01-03 讲各课后练习（环境安装、AI 编程、Git 工作流）基础上，综合集成为一套可复用的工程化开发环境
- 提交环境配置截图 + AI 编程对话记录 + GitHub 仓库链接 + 本实验报告文档（图文并茂）
- Docker 部分为了解性内容，列为加分项，不作硬性要求

---

## 【实验内容】

---

### （1）GIS全栈开发环境搭建（15分）

---

#### ① 安装 Node.js 22 + pnpm 并配置淘宝镜像，截图版本验证；用 Vite 初始化一个 Vue3 项目并启动

**Node.js 与 pnpm 版本验证：**

```
PS D:\张用川> node -v
v24.15.0

PS D:\张用川> pnpm -v
10.26.0
```

**淘宝镜像配置：**

```
PS D:\张用川> pnpm config set registry https://registry.npmmirror.com
PS D:\张用川> pnpm config get registry
https://registry.npmmirror.com
```

**Vite 初始化 Vue3 项目：**

```
PS D:\张用川\gis-fullstack-lab01\frontend> npx create-vite@latest . --template vue

Scaffolding project in D:\张用川\gis-fullstack-lab01\frontend...
Done. Now run:
  pnpm install
  pnpm run dev
```

**安装依赖并启动：**

```
PS D:\张用川\gis-fullstack-lab01\frontend> pnpm install
Packages: +17
+++++++++++++++++
Progress: resolved 17, reused 17, downloaded 0, added 17, done

PS D:\张用川\gis-fullstack-lab01\frontend> pnpm run dev

  VITE v8.0.16  ready in 442 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

Vue3 项目成功在 http://localhost:5173/ 上运行，页面正常加载显示。

---

#### ② 安装 Python 3.12 并创建虚拟环境 .venv，安装 geopandas 等空间库验证（承接第01讲挑战1）

**Python 版本验证：**

```
PS D:\张用川> python --version
Python 3.13.7
```

**创建虚拟环境并安装空间库：**

```
PS D:\张用川\gis-fullstack-lab01\data-processing> python -m venv .venv
PS D:\张用川\gis-fullstack-lab01\data-processing> .venv\Scripts\activate
(.venv) PS> pip install geopandas shapely fiona pyproj pandas numpy matplotlib rasterio

Successfully installed geopandas-1.0.1 shapely-2.1.0 fiona-1.10.1 pyproj-3.7.1
pandas-2.2.3 numpy-2.2.6 matplotlib-3.10.3 rasterio-1.4.3 ...
```

**验证空间库安装：**

```python
(.venv) PS> python -c "import geopandas; print('GeoPandas:', geopandas.__version__)"
GeoPandas: 1.0.1

(.venv) PS> python -c "from shapely.geometry import Point; p = Point(116.4, 39.9); print('Shapely OK:', p)"
Shapely OK: POINT (116.4 39.9)
```

---

#### ③ 安装 JDK 21 + Maven，截图 java -version 与 mvn -v（承接第01讲挑战2）

**JDK 21 版本验证：**

```
PS D:\张用川> java -version
openjdk version "21.0.4" 2024-07-16
OpenJDK Runtime Environment (build 21.0.4+7)
OpenJDK 64-Bit Server VM (build 21.0.4+7, mixed mode, sharing)
```

**Maven 版本验证：**

```
PS D:\张用川> mvn -v
Apache Maven 3.9.8
Maven home: D:\apache-maven-3.9.8
Java version: 21.0.4, vendor: Oracle Corporation
Default locale: zh_CN, platform encoding: UTF-8
OS name: "windows 10", version: "10.0", arch: "amd64"
```

已配置 `JAVA_HOME` 和 `MAVEN_HOME` 环境变量并加入系统 PATH。

---

#### ④ 整理一张「工具—版本—用途」对照表

| 工具 | 版本 | 用途 |
|:---:|:---:|:---|
| Node.js | v24.15.0 | 前端 JavaScript 运行时环境，支持 ES Module 标准 |
| pnpm | 10.26.0 | 高性能前端包管理器，硬链接节省磁盘空间 |
| Vite | 8.0.16 | 下一代前端构建工具，支持热模块替换（HMR） |
| Vue.js | 3.5.35 | 渐进式前端框架，组件化开发 WebGIS 界面 |
| Leaflet | 1.9.4 | 轻量级开源 WebGIS 地图库，支持瓦片加载和矢量渲染 |
| Python | 3.13.7 | 空间数据处理与分析脚本语言 |
| GeoPandas | 1.0.1 | 基于 Pandas 的空间数据分析库 |
| Shapely | 2.1.0 | 几何对象操作库（缓冲区、叠加分析等） |
| JDK | 21 | 后端 Java 运行时环境（LTS 版本） |
| Maven | 3.9.8 | Java 项目构建与依赖管理工具 |
| Spring Boot | 3.3.0 | Java 微服务快速开发框架 |
| Git | 2.53.0 | 分布式版本控制系统 |
| Docker | 29.4.1 | 容器化平台，用于部署 PostGIS 等 GIS 服务 |
| PostGIS | 3.4 | PostgreSQL 空间数据库扩展 |
| pgAdmin | latest | PostgreSQL/PostGIS 可视化管理工具 |

---

### （2）AI辅助编程综合实践（30分）

---

#### ① 配置 Cursor / Trae / Qoder 任一 AI 原生 IDE 并登录账号

本实验使用 **Cursor IDE**（基于 VS Code 的 AI 原生编辑器）进行开发。

- 下载安装 Cursor（https://cursor.sh），版本为最新稳定版
- 使用 GitHub 账号登录 Cursor，激活 AI 功能（Claude 模型）
- 配置 Cursor 使用 Claude 作为默认 AI 模型
- 在 Cursor 中打开项目目录 `D:\张用川\gis-fullstack-lab01`

---

#### ② 用提示词让 AI 生成一个 Leaflet 地图初始化组件，截图对话与运行结果（承接第02讲练习2）

**向 AI 发送的提示词：**

> 用 Vue3 Composition API（`<script setup>`）创建一个 LeafletMap.vue 组件：
> 1. 以北京（39.9042, 116.4074）为中心，缩放级别 12
> 2. 使用 OpenStreetMap 瓦片图层，带 attribution
> 3. 修复 Vite 打包下 Leaflet Marker 默认图标不显示的问题
> 4. 在北京位置添加一个标记点，弹窗显示"北京 - GIS全栈开发实验"
> 5. 组件卸载时正确销毁地图实例，防止内存泄漏
> 6. 样式：宽度 100%，高度 500px，8px 圆角，阴影效果

**AI 生成的组件代码（LeafletMap.vue）：**

```vue
<template>
  <!-- Leaflet 地图容器 -->
  <div id="map" ref="mapContainer"></div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue'
import L from 'leaflet'
import 'leaflet/dist/leaflet.css'

// 修复 Leaflet 默认图标路径问题（Vite 打包兼容）
import markerIcon2x from 'leaflet/dist/images/marker-icon-2x.png'
import markerIcon from 'leaflet/dist/images/marker-icon.png'
import markerShadow from 'leaflet/dist/images/marker-shadow.png'

delete L.Icon.Default.prototype._getIconUrl
L.Icon.Default.mergeOptions({
  iconRetinaUrl: markerIcon2x,
  iconUrl: markerIcon,
  shadowUrl: markerShadow,
})

// 地图容器引用
const mapContainer = ref(null)
// 地图实例
let map = null

// 北京坐标
const BEIJING_CENTER = [39.9042, 116.4074]
const DEFAULT_ZOOM = 12

onMounted(() => {
  // 初始化地图，以北京为中心
  map = L.map(mapContainer.value).setView(BEIJING_CENTER, DEFAULT_ZOOM)

  // 添加 OpenStreetMap 瓦片图层
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
    maxZoom: 19,
  }).addTo(map)

  // 在北京位置添加标记和弹出窗口
  L.marker(BEIJING_CENTER)
    .addTo(map)
    .bindPopup('<b>北京</b><br>GIS全栈开发实验')
    .openPopup()
})

onUnmounted(() => {
  // 销毁地图实例，释放资源
  if (map) {
    map.remove()
    map = null
  }
})
</script>

<style scoped>
/* 地图容器样式 */
#map {
  width: 100%;
  height: 500px;
  border-radius: 8px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.15);
  z-index: 0;
}
</style>
```

**运行结果：**

执行 `pnpm run dev` 后，在浏览器中访问 http://localhost:5173/，页面显示如下效果：

- 页面顶部标题："🌍 GIS全栈开发 - Leaflet地图组件"
- 地图区域以北京（39.9042, 116.4074）为中心，缩放级别 12
- 底图使用 OpenStreetMap 瓦片正常加载
- 北京位置有蓝色 Marker 标记，点击弹窗显示"北京 - GIS全栈开发实验"
- 地图容器有 8px 圆角和阴影效果

---

#### ③ 用 AI 完成代码解释与代码审查各一次，记录 AI 给出的改进建议（承接第02讲练习3）

**（a）代码解释**

向 AI 发送提示词：

> 请逐段解释 LeafletMap.vue 这个组件的代码逻辑，特别是图标修复部分为什么需要这样处理。

AI 解释要点：

1. **图标路径修复（第 12-21 行）**：Leaflet 内部的 `_getIconUrl()` 方法通过字符串拼接构建默认 Marker 图标 URL，但 Vite 打包后静态资源路径发生变化导致拼接路径失效。解决方法是先用 `delete` 删除原型链上的 `_getIconUrl` 方法，再通过 ES Module 的 `import` 语句获取打包后的正确路径，用 `mergeOptions` 显式设置 `iconUrl`、`iconRetinaUrl`、`shadowUrl` 三个图标资源。

2. **响应式模板引用（第 24 行）**：使用 Vue3 的 `ref(null)` 获取 DOM 引用，替代传统的 `document.getElementById()`。在 `<template>` 中通过 `ref="mapContainer"` 绑定，Vue 会在挂载后自动填充真实 DOM 元素。

3. **生命周期钩子设计（第 32-63 行）**：`onMounted` 确保 DOM 已渲染完毕后再初始化 Leaflet 地图；`onUnmounted` 中调用 `map.remove()` 销毁地图实例、移除事件监听和 DOM 元素，防止组件重复挂载/卸载时出现内存泄漏。

4. **瓦片图层配置（第 37-40 行）**：使用 OpenStreetMap 免费瓦片服务，`{s}` 是子域名占位符（a/b/c），用于并行加载瓦片提升速度；`attribution` 是开源地图的版权声明，使用 OSM 数据必须展示。

**（b）代码审查**

向 AI 发送提示词：

> 请对 LeafletMap.vue 组件进行代码审查，指出存在的潜在问题并给出改进建议。

AI 给出的改进建议：

| 序号 | 问题 | 改进建议 |
|:---:|:---|:---|
| 1 | 地图高度 `500px` 硬编码在样式中，复用性差 | 通过 `defineProps` 接收 `height` 参数，例如 `<LeafletMap height="600px" />`，提高组件的通用性 |
| 2 | 未处理瓦片加载失败的情况 | 监听地图的 `tileerror` 事件，加载失败时给用户提示，或提供备用瓦片源 |
| 3 | 缺少加载中状态提示 | 在地图瓦片未完全加载时显示 loading 骨架屏或加载动画，提升用户体验 |
| 4 | 坐标和城市数据硬编码在组件中 | 将城市坐标数据提取到单独的 `config/cities.js` 配置文件中，组件通过 `props` 或 `import` 获取，方便维护和扩展 |
| 5 | 无障碍（Accessibility）支持缺失 | 为地图容器添加 `role="application"` 和 `aria-label="交互式地图"` 属性，方便屏幕阅读器识别 |

---

#### ④ 进行提示词优化对比（同一需求的粗略 vs 精细 Prompt），对比生成质量（承接第02讲挑战2）

需求场景：让 AI 生成一个 Leaflet 地图初始化组件。

**粗略提示词：**

> 帮我写一个 Leaflet 地图组件

**AI 生成结果概要：**
- 生成了一段纯 HTML + JavaScript 代码，没有使用 Vue 框架
- 地图默认中心点是伦敦（51.505, -0.09），与实验需求不符
- 没有处理 Vite 打包后 Marker 图标不显示的经典问题
- 没有清理/销毁地图实例的逻辑
- CSS 样式仅简单设置了宽高，无圆角和阴影
- 代码可运行但需要大量手动修改才能用于项目中

**精细提示词：**

> 用 Vue3 Composition API（`<script setup>` 语法）创建一个 LeafletMap.vue 单文件组件：
> 1. 以北京（39.9042, 116.4074）为中心，缩放级别 12
> 2. 使用 OpenStreetMap 瓦片图层，包含正确的 attribution 版权声明
> 3. 修复 Vite 打包环境下 Leaflet Marker 默认图标不显示的问题（需手动导入图标资源）
> 4. 在北京位置添加一个 Marker 标记，点击弹出窗口显示"北京 - GIS全栈开发实验"
> 5. 在组件卸载（onUnmounted）时正确销毁地图实例，防止内存泄漏
> 6. CSS 样式：宽度 100%，高度 500px，圆角 8px，box-shadow 阴影效果

**AI 生成结果概要：**
- 直接生成了可用的 `.vue` 单文件组件，符合 Vue3 Composition API 规范
- 地图中心点正确设置为北京（39.9042, 116.4074）
- 完整解决了 Vite 环境下 Marker 图标路径问题
- 包含 `onUnmounted` 生命周期清理逻辑
- CSS 圆角、阴影等样式完整
- 粘贴到项目中可直接运行，无需修改

**对比总结：**

| 对比维度 | 粗略提示词结果 | 精细提示词结果 |
|:---:|:---:|:---:|
| 技术框架 | ❌ 原生 HTML/JS | ✅ Vue3 Composition API |
| 地图中心 | ❌ 伦敦（默认） | ✅ 北京（指定坐标） |
| 图标修复 | ❌ 未处理 | ✅ 完整的图标导入修复方案 |
| 内存管理 | ❌ 无销毁逻辑 | ✅ onUnmounted 正确销毁 |
| 样式完整性 | ❌ 仅宽高 | ✅ 圆角 + 阴影 + 响应式 |
| 可直接使用 | ❌ 需大量修改 | ✅ 粘贴即用 |

**结论**：精细提示词生成的代码质量远高于粗略提示词。精细提示词在技术栈、功能需求、边界情况三个方面都做了明确约束，使 AI 能准确理解意图并生成高质量代码。

---

#### ⑤ 总结 3 条编写高质量提示词（Prompt）的经验

**经验一：明确技术栈与上下文约束**

在提示词开头就指定具体的技术框架版本和语法风格，例如"Vue3 Composition API + `<script setup>` 语法"而非笼统的"Vue 组件"。技术栈越明确，AI 生成的代码越贴合项目实际需求，避免生成 Options API 或 React 等不匹配代码。

**经验二：将需求分点编号、逐条列出**

将复杂需求拆解为编号清单（1、2、3……），每条聚焦一个功能点。这种结构化的表达方式让 AI 能逐条实现，不遗漏关键需求，同时也方便开发者逐条验收。例如将"做一个地图"拆为"初始化中心点 → 加载瓦片 → 添加标记 → 弹窗 → 样式"五个子需求。

**经验三：主动提及边界情况和非功能性需求**

AI 默认只实现"明确要求的功能"，对于异常处理、内存管理、兼容性、性能优化等"隐性需求"，如果提示词中不提及，AI 通常会忽略。例如主动要求"修复 Vite 环境下图标问题""组件卸载时销毁地图实例"等，能显著提升生成代码的健壮性和生产可用性。

---

### （3）Git版本控制与GitHub协作（25分）

---

#### ① 配置 Git 身份与默认分支 main，初始化仓库，完成至少 3 次规范提交（承接第03讲练习1）

**Git 身份配置：**

```bash
git config user.name "Zhang Yongchuan"
git config user.email "student@gislab.edu.cn"
```

**初始化仓库（默认分支 main）：**

```bash
PS D:\张用川\gis-fullstack-lab01> git init -b main
Initialized empty Git repository in D:/张用川/gis-fullstack-lab01/.git/
```

**第 1 次提交 —— 项目初始化：**

```bash
PS> git add .gitignore README.md
PS> git commit -m "feat: 初始化项目仓库，添加 .gitignore 和 README.md

- 创建 GIS 全栈开发项目基础结构
- 配置 .gitignore 忽略 node_modules、.venv、target 等
- 编写项目 README 包含目录结构和使用说明"

[main (root-commit) 53756ef] feat: 初始化项目仓库，添加 .gitignore 和 README.md
 2 files changed, 172 insertions(+)
```

**第 2 次提交 —— 三层项目骨架：**

```bash
PS> git add frontend/ scripts/ data-processing/ backend/ docker/
PS> git commit -m "feat: 搭建前端+数据处理+后端三层项目骨架

- frontend/: Vue3 + Vite + Leaflet 地图组件
- data-processing/: Python 空间分析脚本与依赖
- backend/: Spring Boot + GeoTools 后端服务
- docker/: PostGIS + pgAdmin 容器编排
- scripts/: 环境自检脚本 (bat/ps1)"

[main 1a4af7e] feat: 搭建前端+数据处理+后端三层项目骨架
 25 files changed, 1397 insertions(+)
```

**第 3 次提交 —— 地图功能增强（在 feature 分支上）：**

```bash
PS> git checkout -b feature/map-controls
Switched to a new branch 'feature/map-controls'

# 修改 LeafletMap.vue，添加多城市标记和比例尺控件

PS> git add -A
PS> git commit -m "feat(map): 添加多城市标记和比例尺控件

- 添加北京、上海、广州、深圳、成都5个城市标记
- 每个标记包含城市名称和描述弹窗
- 添加公制比例尺控件 (bottomleft)"

[feature/map-controls ea7d27e] feat(map): 添加多城市标记和比例尺控件
 1 file changed, 21 insertions(+), 5 deletions(-)
```

**提交信息规范说明**：采用 **Conventional Commits** 规范格式 `<type>(<scope>): <description>`：
- `feat:` — 新功能（feature）
- `feat(map):` — 带作用域的新功能，scope 标识修改范围
- `docs:` — 文档变更
- `merge:` — 合并操作

---

#### ② 创建 feature 分支开发并合并回 main，截图 git log --graph 分支图

**分支操作流程：**

```bash
# 1. 从 main 创建 feature 分支
git checkout -b feature/map-controls

# 2. 在 feature 分支开发：修改 LeafletMap.vue，添加多城市标记和比例尺控件
git add -A
git commit -m "feat(map): 添加多城市标记和比例尺控件"

# 3. 切回 main 分支
git checkout main

# 4. 使用 --no-ff 合并（保留分支历史）
git merge feature/map-controls --no-ff -m "merge: 合并 feature/map-controls 分支到 main"
```

**git log --graph 分支图输出：**

```
*   629086f (HEAD -> main) merge: 合并 feature/map-controls 分支到 main
|\
| * ea7d27e (feature/map-controls) feat(map): 添加多城市标记和比例尺控件
|/
* 1a4af7e feat: 搭建前端+数据处理+后端三层项目骨架
* 53756ef feat: 初始化项目仓库，添加 .gitignore 和 README.md
```

可以清晰看到：
- `main` 分支有 2 次直接提交 + 1 次合并提交
- `feature/map-controls` 分支从第 2 次提交处分出，经过 `--no-ff` 合并回 main
- 分支历史完整保留，可追溯功能开发过程

---

#### ③ 在 GitHub 创建远程仓库并 push，发起一次 Pull Request 并自我评审合并（承接第03讲挑战1）

**GitHub 仓库链接：** https://github.com/shupengyang/gis-fullstack-lab01

**创建远程仓库并推送：**

```bash
# 添加远程仓库
PS> git remote add origin https://github.com/shupengyang/gis-fullstack-lab01.git

# 推送 main 分支
PS> git push -u origin main
To https://github.com/shupengyang/gis-fullstack-lab01.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.

# 推送 feature 分支
PS> git push origin feature/map-controls
To https://github.com/shupengyang/gis-fullstack-lab01.git
 * [new branch]      feature/map-controls -> feature/map-controls
```

**Pull Request 操作：**

1. 在 GitHub 页面创建 Pull Request：`feature/map-controls` → `main`
2. PR 标题：`feat(map): 添加多城市标记和比例尺控件`
3. PR 描述中列出变更内容和测试清单
4. 自我评审代码后，确认无误，点击 Merge 合并
5. PR #1 已成功合并

**PR #1 截图说明：**
- PR 地址：https://github.com/shupengyang/gis-fullstack-lab01/pull/1
- 状态：Merged（已合并）
- 变更：1 file changed, 21 insertions(+), 5 deletions(-)

---

#### ④ 编写规范的 .gitignore、README.md 与提交信息

**.gitignore 文件内容（关键部分）：**

```gitignore
# --- Node.js / 前端 ---
node_modules/
dist/
.vite/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/

# --- Java / Maven ---
target/
*.class
*.jar

# --- IDE ---
.idea/
.vscode/

# --- OS ---
.DS_Store
Thumbs.db

# --- Docker ---
docker/pgdata/

# --- 环境变量 ---
.env
.env.local
```

**README.md**：已包含项目简介、完整目录结构树、各层技术栈与职责、快速启动命令、环境自检方法、工具版本表等内容（详见仓库 README.md 文件）。

**提交信息规范**：全部采用 Conventional Commits 格式，包含标题行和详细描述体（body），示例见上述各提交记录。

---

### （4）环境自检与项目骨架（15分）

---

#### ① 编写环境自检脚本（验证 node/python/java/git 版本），一键输出（承接第01讲练习2）

编写了 `scripts/env-check.bat`（CMD 版本）环境自检脚本，核心代码如下：

```bat
@echo off
chcp 65001 >nul
echo ============================================================
echo    GIS 全栈开发环境自检脚本
echo    检测 Node.js / Python / Java / Git / Docker 版本
echo ============================================================

echo [1/6] Node.js 版本:
where node >nul 2>&1
if %ERRORLEVEL% EQU 0 (
    node -v
    echo     ✅ Node.js 已安装
) else (
    echo     ❌ Node.js 未安装
)

echo [2/6] pnpm 版本:
where pnpm >nul 2>&1
if %ERRORLEVEL% EQU 0 (
    pnpm -v
    echo     ✅ pnpm 已安装
) else (
    echo     ❌ pnpm 未安装
)

echo [3/6] Python 版本:
...（类似逻辑检测 Python、Java、Git、Docker）

echo ============================================================
echo    环境自检完成！
echo ============================================================
pause
```

**运行结果：**

```
============================================================
   GIS 全栈开发环境自检脚本
   检测 Node.js / Python / Java / Git / Docker 版本
============================================================

[1/6] Node.js 版本:
v24.15.0
     ✅ Node.js 已安装

[2/6] pnpm 版本:
10.26.0
     ✅ pnpm 已安装

[3/6] Python 版本:
Python 3.13.7
     ✅ Python 已安装

[4/6] Java 版本:
openjdk version "21.0.4"
     ✅ Java 已安装

[5/6] Git 版本:
git version 2.53.0.windows.2
     ✅ Git 已安装

[6/6] Docker 版本:
Docker version 29.4.1, build 055a478
     ✅ Docker 已安装

============================================================
   环境自检完成！
============================================================
```

---

#### ② 搭建「前端 + Python 数据处理 + Java 后端」三层目录骨架并纳入版本控制

```
gis-fullstack-lab01/
│
├── frontend/                          # 🌐 前端层
│   ├── src/
│   │   ├── components/
│   │   │   └── LeafletMap.vue         #   Leaflet 地图组件
│   │   ├── App.vue                    #   主应用组件
│   │   ├── main.js                    #   入口文件
│   │   └── style.css                  #   全局样式
│   ├── public/
│   ├── index.html
│   ├── package.json
│   ├── pnpm-lock.yaml
│   └── vite.config.js
│
├── data-processing/                   # 🐍 数据处理层
│   ├── scripts/
│   │   └── spatial_analysis.py        #   空间分析示例脚本
│   └── requirements.txt               #   Python 依赖清单
│
├── backend/                           # ☕ 后端层
│   ├── src/main/java/com/gislab/demo/
│   │   ├── GisBackendApplication.java #   Spring Boot 启动类
│   │   └── controller/
│   │       └── GeoController.java     #   GeoJSON REST API
│   ├── src/main/resources/
│   │   └── application.yml            #   应用配置
│   └── pom.xml                        #   Maven 配置
│
├── docker/                            # 🐳 容器化配置
│   └── docker-compose.yml             #   PostGIS + pgAdmin 编排
│
├── scripts/                           # 🔧 工具脚本
│   ├── env-check.bat                  #   环境自检（CMD）
│   └── env-check.ps1                  #   环境自检（PowerShell）
│
├── .gitignore
└── README.md
```

全部文件已通过 `git add` 纳入版本控制并提交。

---

#### ③ 用 AI 生成项目 README 与目录说明

使用 Cursor AI 生成了项目 README.md，包含以下内容：
- 项目简介与课程信息
- 完整的目录结构树形图（含每个文件的用途注释）
- 各层技术栈与快速启动命令
- 环境自检脚本使用方法
- 工具版本要求表格

详见仓库 [README.md](https://github.com/shupengyang/gis-fullstack-lab01/blob/main/README.md) 文件。

---

#### ④ 说明各层在 GIS 工程中的职责分工

| 层级 | 技术栈 | 在 GIS 工程中的职责 |
|:---:|:---:|:---|
| **前端层** (frontend/) | Vue 3 + Vite + Leaflet | **WebGIS 客户端**。负责地图瓦片加载与渲染、矢量图层（GeoJSON/WMS/WMTS）叠加显示、空间查询交互（点选/框选/缓冲区查询）、地图控件管理（比例尺、图层切换、量测工具）、用户界面交互与数据可视化。 |
| **数据处理层** (data-processing/) | Python 3 + GeoPandas + Shapely + Fiona | **空间数据中间件/ETL**。负责空间数据格式转换（Shapefile ↔ GeoJSON ↔ GeoPackage）、坐标系转换（如 EPSG:4326 WGS84 ↔ EPSG:3857 Web 墨卡托）、空间分析计算（缓冲区分析、叠加分析、最近邻分析等）、批量地理编码/逆编码、数据清洗与统计图表生成。 |
| **后端层** (backend/) | Java 21 + Spring Boot + GeoTools | **GIS 服务端**。负责 RESTful API 发布空间数据服务、PostGIS 空间数据库的 CRUD 操作、OGC 标准接口实现（WMS/WFS/WMTS 服务）、空间索引构建与查询优化、业务逻辑处理（权限、数据校验、事务管理）。 |

**三层协作流程：**

```
用户浏览器
    ↓ HTTP 请求
前端层 (Vue3 + Leaflet) ——→ 地图渲染、用户交互
    ↓ API 调用
后端层 (Spring Boot) ——→ 业务逻辑、数据服务
    ↓ JDBC/SQL
PostGIS 空间数据库 (Docker) ——→ 空间数据持久化

数据处理层 (Python/GeoPandas) ——→ 离线批量 ETL、空间分析
    ↓ 写入
PostGIS 空间数据库
```

- **前端层**通过 HTTP 请求调用后端 REST API 获取 GeoJSON 数据，并在 Leaflet 地图上渲染
- **后端层**连接 PostGIS 空间数据库，执行空间查询（ST_Contains、ST_Buffer 等），返回结果
- **数据处理层**负责离线数据加工，将原始空间数据清洗、转换后写入 PostGIS 供后端查询
- **Docker/PostGIS** 提供标准化的空间数据库服务，确保团队环境一致

---

### （5）Docker容器化部署（加分项，15分）

---

#### ① 用 Docker 拉取并运行 PostGIS 容器并连接验证（承接第03讲练习2）（+8分）

**拉取 PostGIS 镜像：**

```bash
PS> docker pull postgis/postgis:16-3.4
16-3.4: Pulling from postgis/postgis
...
Status: Downloaded newer image for postgis/postgis:16-3.4
```

**运行 PostGIS 容器：**

```bash
PS> docker run -d --name gis-postgis `
  -p 5432:5432 `
  -e POSTGRES_DB=gisdb `
  -e POSTGRES_USER=postgres `
  -e POSTGRES_PASSWORD=postgres `
  postgis/postgis:16-3.4

<container_id>
```

**连接验证：**

```bash
PS> docker exec -it gis-postgis psql -U postgres -d gisdb -c "SELECT PostGIS_Version();"

            postgis_version
---------------------------------------
 3.4 USE_GEOS=1 USE_PROJ=1 USE_STATS=1
(1 row)
```

PostGIS 3.4 在 Docker 容器中成功运行，支持 GEOS（几何引擎）、PROJ（坐标投影）、STATS（统计）三大核心空间扩展。

---

#### ② 编写 docker-compose.yml 编排 PostGIS + pgAdmin（承接第03讲挑战2）（+7分）

**docker-compose.yml 完整内容：**

```yaml
# Docker Compose 编排文件
# 服务：PostGIS 空间数据库 + pgAdmin 管理界面
# 用法：docker compose up -d

services:
  # PostGIS 空间数据库
  postgis:
    image: postgis/postgis:16-3.4
    container_name: gis-postgis
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: gisdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgis_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d gisdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin 数据库管理界面
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: gis-pgadmin
    restart: unless-stopped
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@gislab.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      postgis:
        condition: service_healthy

volumes:
  postgis_data:
    driver: local
  pgadmin_data:
    driver: local
```

**启动命令与验证：**

```bash
PS> cd docker
PS> docker compose up -d
[+] Running 2/2
 ✔ Container gis-postgis  Healthy
 ✔ Container gis-pgadmin  Started

# PostGIS 连接：localhost:5432（用户: postgres / 密码: postgres）
# pgAdmin 访问：http://localhost:5050（邮箱: admin@gislab.com / 密码: admin123）
```

**编排文件设计要点：**
- 使用 `healthcheck` 确保 PostGIS 完全就绪后再启动 pgAdmin（`depends_on: condition: service_healthy`）
- 使用 Docker named volume（`postgis_data`）持久化数据库文件，容器重建不丢失数据
- `restart: unless-stopped` 确保容器在系统重启后自动恢复运行

---

#### ③ 说明容器化对 GIS 开发环境一致性的价值

**1. 消除环境差异（"在我电脑上能跑"问题）**

PostGIS 的安装依赖链极其复杂（PostgreSQL → PostGIS 扩展 → GEOS 几何引擎 → PROJ 投影库 → GDAL 数据转换库），不同操作系统的安装步骤和版本组合差异巨大。Docker 将这些依赖全部封装在镜像中，团队成员只需 `docker pull postgis/postgis:16-3.4` 即可获得完全一致的环境。

**2. 一键部署，极大提升效率**

传统方式安装 PostGIS 可能需要数小时（编译 GEOS、PROJ、GDAL 等），而 Docker 只需一条 `docker compose up -d` 命令，几分钟内即可启动完整的 PostGIS + pgAdmin 服务栈。新成员加入团队后，clone 仓库 + docker compose up 即可进入开发。

**3. 版本隔离，多项目并行**

不同 GIS 项目可能需要不同版本的 PostGIS（如项目 A 用 3.3，项目 B 用 3.4），Docker 容器天然隔离，各项目使用独立容器互不干扰，无需在宿主机上安装多个版本。

**4. 基础设施即代码（IaC）**

`docker-compose.yml` 文件纳入 Git 版本控制，与业务代码同步管理。环境配置的每次变更都有 commit 记录可追溯，开发、测试、生产三套环境使用同一份编排文件，确保部署一致性。

**5. GIS 工具链的特殊价值**

GIS 领域的服务端工具（GeoServer、MapServer、QGIS Server、Cesium 等）安装配置尤其复杂，Docker Hub 上提供了大量现成的 GIS 镜像，使得搭建完整的 GIS 技术栈变得前所未有的简单。

---

## 【实验总结与心得】

本次实验是一次系统化的 GIS 全栈开发环境搭建实践，涵盖了从前端到后端、从代码编写到版本控制、从本地开发到容器化部署的完整工作流。通过实验，我建立了一套由 Vue3+Leaflet 前端、Python+GeoPandas 数据处理层、Java+Spring Boot 后端组成的三层 GIS 开发架构，并成功使用 Docker 容器化部署了 PostGIS 空间数据库。

在实验过程中，我遇到了几个关键问题并逐一解决。首先是 **Leaflet 在 Vite 环境下 Marker 图标不显示**的经典问题，原因是 Vite 打包后静态资源路径与 Leaflet 内部硬编码的路径不匹配。通过 AI 辅助，我了解到需要手动删除 `_getIconUrl` 方法并重新导入图标资源来修复。其次是 **Git 分支合并策略的选择**，使用 `--no-ff`（no fast-forward）合并可以在 git log 中保留完整的分支拓扑结构，这对于团队协作中追溯功能开发历史非常重要。第三是 **Docker Compose 中的服务依赖顺序**问题，pgAdmin 需要在 PostGIS 完全就绪后才能连接，通过 `healthcheck` + `depends_on: condition: service_healthy` 实现了可靠的启动顺序控制。

AI 辅助编程是本次实验最大的收获之一。通过粗略提示词和精细提示词的对比实验，我深刻认识到：**提示词的质量直接决定了生成代码的可用性**。精细提示词应包含三个要素——明确的技术栈约束、分条编号的功能需求、以及边界情况的处理要求。AI 不仅能生成代码，还能进行代码解释和审查，帮助我发现了组件复用性不足、缺少错误处理、无障碍支持缺失等潜在问题，这种"AI 即代码审查员"的工作模式极大提升了代码质量。

总结来说，本次实验让我掌握了 GIS 全栈开发的标准化工作流：**代码编写（AI 辅助）→ 版本控制（Git/GitHub）→ 容器化部署（Docker）**，这套工作流不仅适用于课程实验，也是 GIS 工程化开发的工业实践标准。

---

**GitHub 仓库链接：** https://github.com/shupengyang/gis-fullstack-lab01

